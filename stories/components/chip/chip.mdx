import { Canvas, Meta } from '@storybook/blocks';

import * as ChipStories from './chip.stories.ts';

# Chip

The Chip component is a small, interactive element that represents a single piece of information. It can be used for various purposes, such as displaying tags, categories, or selections.

<Canvas of={ChipStories.Example} className='bg-main' />
