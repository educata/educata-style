import { Canvas, Meta } from '@storybook/blocks';

import * as BackgroundStories from './background.stories.ts';

# Background

Background utilities are used to set the background color of an element.
They can be used to set the background color of an element to a specific color.

<Canvas of={BackgroundStories.Examples} />
