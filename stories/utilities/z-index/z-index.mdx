import { Canvas, Meta } from '@storybook/blocks';

import * as ZIndex from './z-index.stories.ts';

# Z-index

Use our low-level z-index utilities to quickly change the stack level of an element or component.

## Example

Use `z-index` utilities to stack elements on top of one another.
Requires a `position` value other than `static`, which can be set with custom styles or using our position utilities.

<Canvas of={ZIndex.Example} />
