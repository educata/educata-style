import { Canvas, Meta } from '@storybook/blocks';

import * as OverflowStories from './overflow.stories.ts';

# Overflow

Use these shorthand utilities for quickly configuring how content overflows an element.
Adjust the `overflow` property on the fly with four default values and classes. These classes are not responsive by default.

There are four classes available for controlling overflow:

- `.overflow-auto` - Sets the overflow property to `auto`.
- `.overflow-hidden` - Sets the overflow property to `hidden`.
- `.overflow-visible` - Sets the overflow property to `visible`.
- `.overflow-scroll` - Sets the overflow property to `scroll`.

<Canvas of={OverflowStories.Example} />

# Overflow X

Use these shorthand utilities for quickly configuring how content overflows an element on the x-axis.
Adjust the `overflow-x` property on the fly with four default values and classes. These classes are not responsive by default.
There are four classes available for controlling overflow on the x-axis:

- `.overflow-x-auto` - Sets the overflow-x property to `auto`.
- `.overflow-x-hidden` - Sets the overflow-x property to `hidden`.
- `.overflow-x-visible` - Sets the overflow-x property to `visible`.
- `.overflow-x-scroll` - Sets the overflow-x property to `scroll`.

# Overflow Y

Use these shorthand utilities for quickly configuring how content overflows an element on the y-axis.
Adjust the `overflow-y` property on the fly with four default values and classes. These classes are not responsive by default.
There are four classes available for controlling overflow on the y-axis:

- `.overflow-y-auto` - Sets the overflow-y property to `auto`.
- `.overflow-y-hidden` - Sets the overflow-y property to `hidden`.
- `.overflow-y-visible` - Sets the overflow-y property to `visible`.
- `.overflow-y-scroll` - Sets the overflow-y property to `scroll`.
