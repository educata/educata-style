import { Canvas, Meta } from '@storybook/blocks';

import * as FloatStories from './float.stories.ts';

# Float

These utility classes are used to float elements to the left or right of their container, allowing text and inline elements to wrap around them.

<Canvas of={FloatStories.Example} />
